return {
    -- Spec Source
    'petertriho/nvim-scrollbar',

    -- Spec Loading
    enabled = false,
    dependencies = {
        'nvim-hlslens',
        'gitsigns.nvim',
    },

    -- Spec Setup
    config = true,

    -- Spec Lazy Loading
    lazy = false,
}
